[project]
name = "ork-graph"
version = "0.1.0"
description = "ORK Graph notebook environment"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "ipykernel",
    "jupyterlab",
    "numpy",
]
